export function RegistrationContainer({ children }: IRegistrationContainerProps) {
  return (
    <main className="flex w-full min-h-screen flex-col items-center justify-between">
      {children}
    </main>
  )
}
