name: Auto approve
on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v3.2.1
        with:
          review-message: "PR auto-approved by auto-approve-action :+1:"
